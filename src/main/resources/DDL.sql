CREATE TABLE IF NOT EXISTS oauth_entity(
                                           provider,
                                           client_id,
                                           client_secret,
                                           scopes,
                                           auth_url,
                                           redirect_url,
                                           error_page_redirect_url
);